/* Readme:
 * distance is the camera distance for this waypoint (-1 to use camera distance parameter)
 * The average between waypoints will be taken if these values are different
 */

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraWaypoint : MonoBehaviour
{
	public float distance;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
